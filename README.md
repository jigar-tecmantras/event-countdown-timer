# Event Countdown Timer

This repository stores a React frontend inside the `frontend/` directory. Use the commands below from within that folder to run or build the app.

```bash
cd frontend
npm install
npm start
```

For production builds:

```bash
npm run build
```

Testing:

```bash
npm test
```
